package server

import (
	"github.com/gofiber/fiber/v2"
)

func (s *Server) MapPaymentHandlers(a *fiber.App) error {

	// api := a.Group("/ktb-mock")

	// api := a.Group("/order-mock")

	// api := a.Group("/api")
	// v1 := api.Group("/v1")

	return nil
}
